# SDK Directory

This directory contains the Software Development Kit (SDK) and stable public interfaces of the Sentinel platform.

## Folder Philosophy

The SDK contains the C-style ABI definitions, JSON-RPC schemas, and C++ abstract interface bases required to write plugins and clients for Sentinel.
Any modification to this directory must follow strict semantic versioning (SemVer 2.0.0) guidelines to maintain backward compatibility.
