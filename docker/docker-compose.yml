version: '3.8'

services:
  frontend:
    build:
      context: ..
      dockerfile: docker/frontend/Dockerfile
    ports:
      - '5173:5173'
    environment:
      - NODE_ENV=production
    restart: always
